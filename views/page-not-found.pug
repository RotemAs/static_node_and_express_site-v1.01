extends layout 
block content 
    div.centerIt
        h1 #{err.message} 
        h2 #{err.status} 
        h3
            a(href='/') 
             img.errorImg(src='static/assets/img/error.gif')
        pre #{err.stack}
